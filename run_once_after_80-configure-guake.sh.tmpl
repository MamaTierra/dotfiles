#!/bin/bash

echo "*** Configure guake ***"

# Load guake config
if [[ -z "${RUNNING_IN_DOCKER}" ]]; then
    dconf load /apps/guake/ < ${HOME}/.guake/dconf-guake-dump.txt
fi

echo "*** Configure guake done ***"